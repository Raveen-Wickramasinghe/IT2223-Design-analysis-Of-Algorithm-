01..Directed Graph with Weighted Edges

Description
Nodes and Edges: This is a directed graph where s represents the source nodes and t represents the target nodes. The weights array contains the weights of the edges between the corresponding nodes.

Edge Labeling: The edge labels display the weights of the connections.

Node Coloring: All nodes are colored red using the NodeColor property.

Visualization
The graph is visualized with labeled edges showing the weights and red-colored nodes.


****Graph 2: Directed Graph with Weighted Edges and Force-Directed
Layout
Description
Nodes and Edges: Similar to the previous graph, it is a directed graph with edge weights.

Force Layout: The layout of the graph is generated using the force-directed layout algorithm, which attempts to position nodes so that edges do not overlap, and the graph is visually appealing.

Edge Labeling: The edge labels display the weights of the edges.

Visualization
The graph uses the force layout to visually arrange the nodes and edges in a way that avoids overlap.


Graph 3: Undirected Graph with Custom Node Colors

Description
Nodes and Edges: This is an undirected graph where s and t define the connections between nodes.

Node Coloring: Custom colors are applied to each node. Each node has a unique color defined in the nodeColors matrix.

Edge Width: The edges are plotted with a line width of 1.5.

Visualization
The graph is visualized with nodes colored according to the nodeColors matrix, and edges are displayed with a specified line width.

Graph 4: Directed Graph with Custom Node Colors and Force Layout

Description
Nodes and Edges: This graph is a directed graph with edge weights applied to the edges.

Node Colors: The nodes are colored according to the custom nodeColors matrix.

Edge Labeling: The edge weights are shown on the plot.

Edge Width: The edges are displayed with a line width of 1.5.

Visualization
The graph is displayed with labeled edges showing the weights and colored nodes. The force-directed layout is used to visually arrange the nodes.